# CLAUDE.md

映画レビューサイト **cinemind.jp** の運営リポジトリ。
自己ホスト型WordPress（エックスサーバー）で運用している。

---

## ユーザーとのやりとり

- **敬語で書く。**
- **短く書く。** 結論を先に、理由は必要な分だけ
- **一度に大量に出さない。** 1ステップずつ進める
- **実行前に、何をするかを具体的に言う。** 「実行します」だけで動かない
- 判断を仰ぐときは、選択肢を2〜3個に絞って示す

---

## WordPressへの接続

環境変数 `WP_USER` / `WP_APP_PASSWORD` にログインIDとアプリケーションパスワードが入っている。

```bash
curl -u "$WP_USER:$WP_APP_PASSWORD" "https://cinemind.jp/wp-json/wp/v2/posts?status=draft"
```

- **環境変数の値は絶対に出力しない・ファイルに書かない・コミットしない**
- 権限は administrator。記事の作成・編集・公開・削除、画像アップロード、カテゴリー管理が可能
- **書き込み（POST / DELETE）は、必ずユーザーの承認を得てから行う**
- 記事は**下書きまで作って止める**。公開はユーザーが確認してから

---

## サイトの現状（2026年9月時点）

| 項目 | 状態 |
|---|---|
| 公開記事 | 24本（すべて洋画。MCU作品が中心） |
| 下書き | 1本（「Amazon S3」＝映画と無関係。要確認） |
| PV | ほぼゼロ |
| インデックス | されている（技術的な問題はない） |
| 記事の中身 | **ほぼAI生成**。あらすじ＋一般的な感想で構成されている |

### 技術面（問題なし）

- パーマリンクは記事ごとに設定済み（`https://cinemind.jp/captain2/` 形式）
- robots.txt・サイトマップともに正常
- SEO SIMPLE PACK、Pochipp（アフィリンク）導入済み

---

## PVがゼロの理由

記事24本すべてが「『作品名』感想・レビュー」という形。
これは映画系で**最も競合が強い検索語**で、Filmarks・映画.com・Yahoo!映画・大手アフィリエイトサイトが上位を独占している。
開設10ヶ月・24記事の個人サイトが正面から戦って勝てる相手ではない。

**PVゼロは不具合ではなく、狙った検索語が取れないものだったという結果。**
方針を変えないと記事を増やしても同じ。

---

## 方針

### 記事の作り方（最重要）

AI生成そのものが問題なのではない。評価されないのは**独自の価値がない量産コンテンツ**。
Googleはレビュー記事について「実際に体験した人だけが書ける具体性」を明確に評価基準にしている。

**Claudeが単独で書くと、今と同じ結果になる。Claudeは作品を観ていないため。**

| 担当 | やること |
|---|---|
| ユーザー | 観て気づいたことをメモで渡す（1記事5〜10分） |
| Claude | メモを軸に構成・執筆・推敲する |

欲しいメモの粒度:

- 「ラスト20分、〇〇が△△する場面で鳥肌が立った」
- 「1回目は気づかなかったが、2回目で〇〇の伏線だとわかった」
- 「正直、中盤30分は退屈だった」
- 「字幕版のほうが良かった。吹替は△△が合わない」

**ネガティブな感想ほど価値がある。** 他のサイトが書かないため。

### 狙う検索語

| 方向 | 内容 | 優先度 |
|---|---|---|
| A. まとめ・ガイド記事 | 「マーベル 見る順番 時系列」など。既存24記事のハブになり、内部リンクで全体が繋がる | 高 |
| B. 気分×条件の複合語 | 「疲れた時に見る映画 Netflix」など。既存カテゴリー「現実逃避したい」「疲れた時」が活きる | 中 |
| C. 競合の薄い作品 | 話題の新作ではなく、レビューが少ない旧作・マイナー作品 | 低 |

### 既存24記事の扱い

分析結果（`docs/editorial-analysis.md`）を見てから、次の3つに仕分ける。

- リライトして残す
- まとめ記事に統合する
- 削除または noindex にする

**薄い記事が大量にあるとサイト全体の評価を下げる。** 整理は必要。

### 土台の整備

- 運営者情報ページを作る（誰が書いているか不明なサイトは評価されにくい）
- 記事どうしの内部リンクを繋ぐ
- メタディスクリプションを記事ごとに設定する

---

## セッションの役割分担

**メインセッションは総括役。** ユーザーと方針を議論して決めるのが仕事。
**実作業は子セッションに任せる**（記事の執筆、調査、既存記事の分析など）。

### 子セッションのモデル選択

| 用途 | モデル |
|---|---|
| 基本すべて | **Sonnet** |
| 本当に複雑な処理のときだけ | Opus |

`create_session` の `model` を明示的に指定すること。省略すると親のモデルを引き継ぐ。

### 子セッションへの発注時の注意

**子セッションは `git push` が認証で失敗することがある。**
ファイルを残させたい場合は、GitHub API（`create_or_update_file`）を使わせること。
また、親から子へメッセージを送れない場合があるため、**発注時に必要な情報をすべて渡しきる**。

### 記事は3段階で作る

| 段階 | 担当 | やること |
|---|---|---|
| 1. 執筆 | Sonnet | ユーザーのメモを軸に記事を書く |
| 2. 検証 | **Opus（別セッション）** | 白紙で読み、事実誤認・作品情報の誤りを探す |
| 3. 判断 | メイン | 報告を見て下書き投稿する |

書いた本人に検証させない。自分の思い込みが見えないため。

---

## ディレクトリ

```
docs/         方針メモ・分析結果
articles/     記事の下書き（Markdown）
```

---

## 扱いに注意が必要なもの

- **作品情報**（監督・公開年・配信先）は必ず裏を取る。AIの記憶で書かない
- **配信状況**は変動する。「Netflixで配信中」と断定する前に確認する
- **ネタバレ**は注意書きを入れる
